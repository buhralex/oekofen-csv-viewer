---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - uPlot.iife.min.js
  - uPlot.min.css
  - papaparse.min.js
autonomous: true
requirements: [INTF-01, INTF-02]

must_haves:
  truths:
    - "index.html opens in a browser without errors in the console"
    - "uPlot and PapaParse are loadable as <script> tags from local vendor files"
    - "AppState singleton is accessible at window.AppState in the browser console"
    - "UI text (labels, placeholder) is in English"
  artifacts:
    - path: "index.html"
      provides: "App entry point — HTML shell with embedded JS and CSS"
      min_lines: 80
      contains: "AppState"
    - path: "uPlot.iife.min.js"
      provides: "uPlot 1.6.32 IIFE bundle"
      min_lines: 1
    - path: "uPlot.min.css"
      provides: "uPlot default chart styles"
      min_lines: 1
    - path: "papaparse.min.js"
      provides: "PapaParse 5.5.3 CSV parser"
      min_lines: 1
  key_links:
    - from: "index.html <script src>"
      to: "uPlot.iife.min.js"
      via: "script tag in <head>"
      pattern: "src=[\"']uPlot\\.iife\\.min\\.js[\"']"
    - from: "index.html <script src>"
      to: "papaparse.min.js"
      via: "script tag in <head>"
      pattern: "src=[\"']papaparse\\.min\\.js[\"']"
    - from: "index.html inline <script>"
      to: "window.AppState"
      via: "const AppState = { ... }; window.AppState = AppState;"
      pattern: "window\\.AppState"
---

<objective>
Create the single-file web application scaffold: the HTML entry point, vendored library files (uPlot 1.6.32 and PapaParse 5.5.3), and the AppState singleton that all subsequent plans build on.

Purpose: Establishes the foundation every other plan depends on. Everything lives in or alongside index.html — there is no build step, no bundler, no framework. This is the skeleton the file-loading and parse pipeline will be added to.

Output: index.html (HTML shell + embedded JS skeleton), uPlot.iife.min.js, uPlot.min.css, papaparse.min.js — all in the project root.
</objective>

<execution_context>
@C:/Users/buhra/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/buhra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download and vendor uPlot 1.6.32 and PapaParse 5.5.3</name>
  <files>uPlot.iife.min.js, uPlot.min.css, papaparse.min.js</files>
  <action>
    Download the exact pinned versions into the project root directory:

    1. uPlot 1.6.32 IIFE bundle:
       URL: https://unpkg.com/uplot@1.6.32/dist/uPlot.iife.min.js
       Save as: uPlot.iife.min.js

    2. uPlot CSS:
       URL: https://unpkg.com/uplot@1.6.32/dist/uPlot.min.css
       Save as: uPlot.min.css

    3. PapaParse 5.5.3:
       URL: https://unpkg.com/papaparse@5.5.3/papaparse.min.js
       Save as: papaparse.min.js

    Use fetch/curl/wget to download. Verify each file is non-empty after download.
    These files MUST be local — the app is client-side only, no CDN dependency at runtime.
  </action>
  <verify>
    Check each file exists and is non-empty:
    - uPlot.iife.min.js should be ~49 KB
    - uPlot.min.css should be ~2 KB
    - papaparse.min.js should be ~51 KB

    Grep for version strings:
    - grep "1.6.32" uPlot.iife.min.js (should match)
    - grep "5.5" papaparse.min.js (should match)
  </verify>
  <done>Three vendor files exist in project root, non-empty, correct versions.</done>
</task>

<task type="auto">
  <name>Task 2: Create index.html — scaffold, AppState, and initial UI layout</name>
  <files>index.html</files>
  <action>
    Create index.html in the project root. This is the ENTIRE application — all JS is inline in a single &lt;script&gt; tag. Do NOT create separate .js files.

    STRUCTURE:
    ```
    &lt;!DOCTYPE html&gt;
    &lt;html lang="en"&gt;
    &lt;head&gt;
      &lt;meta charset="UTF-8"&gt;
      &lt;meta name="viewport" content="width=device-width, initial-scale=1.0"&gt;
      &lt;title&gt;OekoFEN CSV Viewer&lt;/title&gt;
      &lt;link rel="stylesheet" href="uPlot.min.css"&gt;
      &lt;style&gt;
        /* All app CSS here */
      &lt;/style&gt;
    &lt;/head&gt;
    &lt;body&gt;
      &lt;!-- Drop zone: full-page when no file loaded --&gt;
      &lt;div id="drop-zone"&gt;
        &lt;div id="drop-message"&gt;
          &lt;p&gt;Drop an OekoFEN CSV file here&lt;/p&gt;
          &lt;p&gt;or&lt;/p&gt;
          &lt;button id="pick-file-btn"&gt;Select File&lt;/button&gt;
          &lt;input type="file" id="file-input" accept=".csv" style="display:none"&gt;
        &lt;/div&gt;
      &lt;/div&gt;

      &lt;!-- Header bar: compact, shown after file loads --&gt;
      &lt;header id="app-header" style="display:none"&gt;
        &lt;span id="header-filename"&gt;&lt;/span&gt;
        &lt;button id="load-another-btn"&gt;Load Another&lt;/button&gt;
      &lt;/header&gt;

      &lt;!-- Data summary: shown after load --&gt;
      &lt;div id="data-summary" style="display:none"&gt;&lt;/div&gt;

      &lt;!-- Toast notification container --&gt;
      &lt;div id="toast-container"&gt;&lt;/div&gt;

      &lt;!-- Chart area: Phase 2+ --&gt;
      &lt;div id="chart-area" style="display:none"&gt;&lt;/div&gt;

      &lt;!-- Status bar --&gt;
      &lt;div id="status-bar"&gt;&lt;/div&gt;

      &lt;script src="uPlot.iife.min.js"&gt;&lt;/script&gt;
      &lt;script src="papaparse.min.js"&gt;&lt;/script&gt;
      &lt;script&gt;
        'use strict';

        // ─── App State ────────────────────────────────────────────────
        const AppState = {
          dataModel: null,   // { timestamps: Float64Array, columns: Column[] }
          filename: null,
          fileDate: null,
          // Phase 2+ view state: visibleColumns, zoomRange
        };
        window.AppState = AppState; // expose for debugging

        // ─── Column descriptor shape (for Phase 2+) ───────────────────
        // {
        //   rawName: string,        // e.g. "HK1 VL Ist[°C]"
        //   displayName: string,    // trimmed, same as rawName (no translation)
        //   unit: string|null,      // e.g. "°C"
        //   group: string,          // e.g. "Heating Circuit (HK1)"
        //   type: 'continuous'|'discrete',
        //   data: number[],         // parallel to timestamps array
        // }

      &lt;/script&gt;
    &lt;/body&gt;
    &lt;/html&gt;
    ```

    VISUAL DESIGN (Claude's discretion — use dark theme for chart readability):
    - Background: #1a1a2e or similar dark navy
    - Text: #e0e0e0 light grey
    - Accent: #4fc3f7 light blue (buttons, highlights)
    - Drop zone: full-viewport centered layout with dashed border (#4fc3f7) that pulses on dragover
    - After load: drop zone becomes 50px-high header bar, chart area takes over the rest of the viewport
    - Font: system-ui, -apple-system, sans-serif

    EMPTY STATES to stub in (functionality added in Plans 02 and 03):
    - #drop-zone starts visible, full screen
    - #app-header starts hidden
    - #data-summary starts hidden
    - #chart-area starts hidden
    - #status-bar is always visible at bottom (empty text initially)
    - #toast-container positioned fixed top-right

    REQUIREMENT INTF-01: All UI labels must be in English ("Drop an OekoFEN CSV file here", "Select File", "Load Another").
    REQUIREMENT INTF-02: Column names displayed unchanged from CSV (German names, no translation). This is enforced by the data model: rawName and displayName are the same string, just trimmed.
  </action>
  <verify>
    Open index.html in a browser:
    1. Page loads without console errors
    2. Dark theme visible — not default white browser background
    3. Drop zone centered on screen with "Drop an OekoFEN CSV file here" text in English
    4. "Select File" button visible
    5. In browser console: type `AppState` and press Enter — should return the AppState object (not undefined)
    6. In browser console: type `uPlot` — should return the uPlot constructor (not undefined)
    7. In browser console: type `Papa` — should return the PapaParse object (not undefined)
  </verify>
  <done>
    index.html opens in browser with dark theme drop zone UI, all text in English. AppState, uPlot, and Papa are accessible in the browser console. No console errors.
  </done>
</task>

</tasks>

<verification>
Open index.html in a browser (no server needed — file:// protocol). Confirm:
1. Page renders without white flash or console errors
2. Drop zone UI is centered, dark themed, text is in English
3. Console access: AppState is the object, uPlot is the constructor, Papa is the parser
4. All vendor files (uPlot.iife.min.js, uPlot.min.css, papaparse.min.js) load without 404 errors in Network tab
</verification>

<success_criteria>
- index.html exists with dark theme scaffold, English UI text, AppState singleton
- uPlot.iife.min.js (1.6.32), uPlot.min.css, papaparse.min.js (5.5.3) vendored in project root
- No external CDN dependencies (all local)
- INTF-01 satisfied: UI labels are in English
- INTF-02 foundation laid: AppState column descriptor shape preserves rawName without translation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following the summary template.
</output>
