---
phase: 06-download-ui-and-error-handling
plan: "03"
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified: []
autonomous: false
requirements: [CONN-01, CONN-02, CONN-03, ERR-01, ERR-02, ERR-03, ERR-04]

must_haves:
  truths:
    - "User can select a log period and click Download from Heater to fetch real CSV data from the heater"
    - "Loaded CSV data renders identically to drag-and-dropped CSV data from the same heater log"
    - "Opening index.html directly from the filesystem (file://) and clicking Download shows the python -m http.server error"
    - "Clicking Download twice within 2500ms shows the rate-limit warning on the second click"
    - "When heater is unreachable the user sees an actionable error with Settings guidance"
  artifacts:
    - path: "index.html"
      provides: "Complete Phase 6 implementation verified against real or simulated failure conditions"
      contains: "async function fetchCsv"
  key_links:
    - from: "Download button click"
      to: "CSV rendered in chart"
      via: "fetchCsv() -> response.arrayBuffer() -> TextDecoder('windows-1252') -> onCsvStringAccepted() -> createChart()"
      pattern: "onCsvStringAccepted"
---

<objective>
Empirical verification of all Phase 6 behavior paths: download happy path against a real OekoFEN heater, all five error conditions, and regression check that file-drop still works.

Purpose: The CORS behavior of the OekoFEN heater is empirically unverified (STATE.md blocker). This checkpoint surfaces whether the heater returns CORS headers or not, and captures the finding for STATE.md.
Output: Human confirmation that download works (or documents which specific error the heater returns), plus regression confirmation that file-drop still works.
</objective>

<execution_context>
@C:/Users/buhra/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/buhra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-download-ui-and-error-handling/06-RESEARCH.md
@index.html
@.planning/phases/06-download-ui-and-error-handling/06-01-SUMMARY.md
@.planning/phases/06-download-ui-and-error-handling/06-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Empirical device and error-path verification</name>
  <action>Human verification required — see how-to-verify for exact test steps. No code changes in this task.</action>
  <verify>
    <automated>grep -n "async function fetchCsv\|showFetchControls\|hideFetchControls\|fetch-controls" "C:/Users/buhra/source/repos/oekofen_csv_viewer/OekoFEN_CSV_Viewer/index.html" | head -10</automated>
    <manual>See how-to-verify section below for the full 7-test checklist.</manual>
  </verify>
  <done>User types "approved" with CORS behavior observation, rate-limit guard confirmation, and file-drop regression result.</done>
  <what-built>
    Complete Phase 6 implementation:
    - fetchCsv() with file:// pre-flight, 2500ms rate-limit guard, 10s timeout, HTTP status mapping
    - Drop zone fetch controls: log period selector (Today/Yesterday/Log 0-3) + Download from Heater button
    - show/hide logic driven by _settings.ip && _settings.password
    - All error paths: file:// origin, CORS/network failure, timeout, HTTP 401, HTTP 404
  </what-built>
  <how-to-verify>
    Serve the app: open a terminal in the project directory and run:
      python -m http.server 8080
    Then open: http://localhost:8080

    **Test A — Controls visibility:**
    1. Open Settings (gear icon). If no heater configured: fetch controls should be hidden.
    2. Enter your heater IP, port, and password. Click Save.
    3. Confirm: selector and "Download from Heater" button appear below the gear icon in the drop zone.

    **Test B — Happy path (requires real OekoFEN heater on LAN):**
    4. Select "Today" or "Log 0" from the selector.
    5. Click "Download from Heater".
    6. Expected: chart loads with today's data — identical visual result to dragging the same file from the heater's file export.
    7. If it works: note "heater supports CORS" in resume signal.
    8. If it fails with a CORS/network toast: this is expected — see Test C.

    **Test C — CORS error (if heater returns no CORS headers):**
    9. After a "Cannot reach heater" toast appears, note the exact message shown.
    10. DevTools Network tab: confirm the request was made; check response headers for Access-Control-Allow-Origin.
    11. If no CORS header: the error message is correct — confirm "Cannot reach heater. If the heater does not support browser requests (CORS)..." appears.

    **Test D — Rate limit guard:**
    12. Click "Download from Heater" (any result).
    13. Immediately click again (within 2500ms).
    14. Expected: second click shows "Please wait before downloading again." warning toast. Button does NOT make a second request.

    **Test E — file:// origin error:**
    15. Double-click index.html directly (opens as file:///...).
    16. Enter heater settings, click Download.
    17. Expected: "Network fetch requires a web server. Open a terminal and run: python -m http.server 8080..." error toast appears. No network request is made.

    **Test F — Regression (file-drop still works):**
    18. Back at http://localhost:8080, drag a known-good OekoFEN CSV file onto the drop zone.
    19. Expected: chart renders correctly — identical to v1.0 behavior.

    **Test G — Heater unreachable (optional — use wrong IP):**
    20. In Settings, change IP to 192.168.99.99 (unreachable).
    21. Click Download. Wait up to 10 seconds.
    22. Expected: "Heater did not respond in 10 seconds. Check the IP address and port in Settings." error toast.
  </how-to-verify>
  <resume-signal>
    Type "approved" and include:
    - Test B result: "heater supports CORS" OR "heater blocks CORS — expected error shown"
    - Test D result: "rate limit guard works" OR describe what happened
    - Test F result: "file-drop still works" OR describe regression
    - Any unexpected behavior observed

    If issues found, describe what failed and Claude will create a gap-closure plan.
  </resume-signal>
</task>

</tasks>

<verification>
This plan IS the verification. Completion criteria:
- User has confirmed all test scenarios (A through F) have expected outcomes
- CORS behavior of heater is documented in the resume signal
- File-drop regression is confirmed
- STATE.md blocker "CORS header behavior of OekoFEN heater is empirically unverified" is resolved (either confirmed CORS works, or confirmed it doesn't and the error UX is correct)
</verification>

<success_criteria>
- User types "approved" with CORS behavior observation
- File-drop (v1.0) regression confirmed passing
- Rate-limit guard confirmed working
- file:// origin error confirmed working
- Phase 6 requirements CONN-01, CONN-02, CONN-03, ERR-01, ERR-02, ERR-03, ERR-04 are all verified or their error paths are confirmed correct
</success_criteria>

<output>
After completion, create `.planning/phases/06-download-ui-and-error-handling/06-03-SUMMARY.md`

Include in the summary:
- CORS behavior result (heater supports/blocks CORS)
- Update the "CORS header behavior of OekoFEN heater is empirically unverified" blocker in STATE.md with the finding
- Note Chrome LNA status if observed during testing
</output>
